import { Navbar } from '../Navbar'

export const Header = () => (
  <header className="sticky top-0 z-[100] bg-[rgba(255,255,255,0.8)] py-5 shadow-md backdrop-blur-sm">
    <div className="container">
      <Navbar />
    </div>
  </header>
)
